# --- Public config (safe in the front-end; protected by Row Level Security) ---
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key-here

# --- Server-side ONLY: never put these in the front-end, the repo, or chat ---
# GEMINI_API_KEY            -> set as a Supabase Edge Function secret (supabase secrets set)
# SUPABASE_SERVICE_ROLE_KEY -> use only in the Supabase dashboard / admin tasks
